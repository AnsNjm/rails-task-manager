<h3>Task :</h3>
<h2><%= @task.title %></h2>
<h3>details:</h3>
<h2><%= @task.details %></h2>
<h3>is it completed : </h3>
<h2><%= @task.completed %> no</h2>

<%= link_to "Edit the task", edit_task_path %>
<%= link_to "GO back to tasks list", tasks_path %>
<%= link_to "Delete the task",
 task_path(@task),
 data: {turbo_method: :delete, turbo_confirm: "are you sure?"} %>
